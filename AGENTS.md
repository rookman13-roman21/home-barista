# home-barista — карточка проекта

> Статус: active/support
> Обновлено: 2026-09-14
> ⚠️ Репозиторий ПУБЛИЧНЫЙ — не коммитить секреты.

- **Что это:** отдельный проект «домашний бариста»; физически вложен в `barista-course/`, но это САМОСТОЯТЕЛЬНЫЙ git-репозиторий.
- **Production:** `https://baristaschool.ru/home_barista`, Tilda page `61177677`.
- **Как проверить:** открыть страницу, нажать любую из четырёх кнопок «Записаться онлайн», проверить выбор solo/pair и восстановление прокрутки после закрытия.
- **Как деплоится:** HTML-блоки вручную переносятся в Tilda; backend и конфиг курса выпускаются из `schedule-online/basic-barista-booking`.
- **Откат Tilda:** вернуть предыдущий сохранённый вариант изменённого HTML-блока и опубликовать страницу повторно.
- **Ловушка:** не коммитить его файлы из родительского `barista-course` и наоборот.
- **Соседние системы:** `schedule-online/basic-barista-booking`, yClients, Битрикс и страницы `barista-course` (`home-barista-online/` в родителе — другая сущность, страница Tilda).
- **Релиз записи через Дашборд MBS:** 14.09.2026 PR #1 слит в `main` (`9f87ef3`), Tilda page опубликована и принята Романом. Четыре CTA открывают встроенный popup; старой прямой ссылки yClients нет. Backend-конфиг выпущен из `schedule-online` PR #32 (`c3598a4`) с `record_create_mode=manager`, общим интервалом `12600`, техническим перерывом `1800` и `save_if_busy=false`. Групповые курсы не менялись. Реальная запись специально не создавалась: `created_user_id=13988193` и создателя сделки Битрикс проверить на первой естественной записи. Backend rollback: `/opt/basic-barista-booking-deploy-backups/20260914T0020-c3598a4-course-creator/`.
- **Главная дока:** эта карточка `AGENTS.md`; сборный источник страницы — `index.html`, публикуемые блоки — `tilda-blocks/`.
